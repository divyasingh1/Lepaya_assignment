//As and when the project will grow
//Base service will come handy when we will have common features that could be used and modified by all other child classes
//Inheritence
class BaseService {
    constructor() {
    }
}

module.exports = BaseService;
